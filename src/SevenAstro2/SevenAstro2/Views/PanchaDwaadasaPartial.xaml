<UserControl x:Class="SevenAstro2.Views.PanchaDwaadasaPartial"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             x:ClassModifier="internal"
             xmlns:local="clr-namespace:SevenAstro2"
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <DataGrid Name="dgPanchaDwaadasa" AlternationCount="2"
                  VerticalAlignment="Stretch" HorizontalAlignment="Stretch" 
                  ItemsSource="{Binding}" 
                  AutoGenerateColumns="False" HeadersVisibility="Column">
            <DataGrid.Columns>
                <DataGridTextColumn Header="" IsReadOnly="True" Binding="{Binding Planet}" MinWidth="24" />
                <DataGridTextColumn Header="PVB" IsReadOnly="True" Binding="{Binding Pancha}" MinWidth="25" />
                <DataGridTextColumn Header="DVB" IsReadOnly="True" Binding="{Binding Dwaadasa}" MinWidth="25" />
                <!-- Vargeeya Bala -->
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
